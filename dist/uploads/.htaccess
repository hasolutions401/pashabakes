# Cookie photos only — never run scripts from this folder.
Options -Indexes -ExecCGI
<FilesMatch "\.(php|phtml|phar|pl|py|cgi|sh|html?|svg|js)$">
  Require all denied
</FilesMatch>
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
</IfModule>
